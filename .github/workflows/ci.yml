name: ci
on:
  push:
    branches:
      - native
jobs:
  test:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
#       - name: Set up JDK 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'
      - name: ls
        run: pwd && ls $GITHUB_WORKSPACE
